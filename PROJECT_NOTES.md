# WB Analiz — Заметки проекта

## Цель проекта
Автоматизация мониторинга остатков и рекомендаций по изменению цен для продавца Wildberries.

---

## Что сделано ✅

### 1. Подключение к WB API
- Токен: `nemov_token.txt` (вторая строка)
- Авторизация: заголовок `Authorization: {токен}`
- Документация: `WB_API_ENDPOINTS.md`

### 2. Выгрузки данных (январь 2026)
| Файл | Записей | Описание |
|------|---------|----------|
| `sales_january_2026.csv` | 16 501 | Продажи |
| `orders_january_2026.csv` | 20 281 | Заказы |
| `returns_january_2026.csv` | 75 | Возвраты |
| `stocks_january_2026.csv` | 1 390 | Остатки |
| `incomes_january_2026.csv` | 110 | Поставки |

### 3. Скрипт рекомендаций по ценам
- Файл: `price_analysis.ipynb`
- Выход: `price_report.xlsx`
- Логика: шкала повышения цены при остатке < 7 дней

### 4. Телеграм-бот
- Бот: `@wb_dobrodom_bot`
- Отправляет Excel-отчёт ежедневно в **8:00 МСК**
- Принимает фидбек (👍👎🤷 или текст)
- Запуск: `./start.sh` (Docker)

---

## Бизнес-логика (из интервью)

### Формула остатка в днях:
```
Остаток (дней) = Кол-во на складе / Среднее заказов в день
```

### Шкала повышения цены:
| Остаток (дней) | Повышение цены |
|----------------|----------------|
| 7              | +5%            |
| 6              | +10%           |
| 5              | +15%           |
| 4              | +25%           |
| 3              | +30%           |
| 2              | +40%           |
| 1              | +50%           |
| 0              | +50% (макс)    |

### Параметры:
- Среднее считается за **7 дней**
- Поставки заказываются **раз в неделю**
- Отчёт нужен **ежедневно** (ночью расчёт → утром таблица)

---

## Структура проекта

```
wb_analiz/
├── wb_api.py                  ← функции WB API
├── price_analysis.ipynb       ← ноутбук с анализом
├── nemov_token.txt            ← токен WB (не коммитить!)
├── .env                       ← токены бота (не коммитить!)
│
├── bot/                       ← Телеграм-бот
│   ├── config.py              ← настройки
│   ├── report.py              ← генерация Excel
│   ├── handlers.py            ← обработчики команд
│   └── main.py                ← точка входа
│
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── start.sh                   ← скрипт запуска
│
├── reports/                   ← сгенерированные отчёты
├── feedback/                  ← фидбек от пользователя
│   └── feedback.csv
│
└── docs/
    └── WB_API_ENDPOINTS.md    ← документация API
```

---

## TODO (что дальше)

- [x] Автозапуск скрипта → реализовано через APScheduler (8:00 МСК)
- [x] Уведомления в Telegram → бот отправляет Excel-отчёт
- [x] Свежие данные через API → бот берёт данные из WB API
- [ ] Деплой на сервер (VPS/облако)
- [ ] Опционально: автоизменение цен через API

---

## Что НЕ нужно / удалено

*(пока пусто — сюда записывать что пробовали и не подошло)*

---

*Последнее обновление: 31 января 2026*
