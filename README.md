# WB Analiz Bot

Телеграм-бот для ежедневной отправки отчётов по ценам Wildberries.

## Быстрый старт

```bash
# 1. Создать .env файл
cp .env.example .env

# 2. Заполнить переменные в .env:
#    TELEGRAM_TOKEN=токен_от_BotFather
#    CHAT_ID=ваш_telegram_id

# 3. Запустить
./start.sh
```

## Команды управления

```bash
# Логи
docker-compose logs -f

# Остановить
docker-compose down

# Перезапустить
docker-compose restart
```

## Что делает бот

- **08:00 МСК** — автоматически отправляет Excel-отчёт
- `/start` — приветствие
- `/report` — получить отчёт сейчас
- Принимает фидбек: 👍 👎 🤷 или текст

## Файлы

- `reports/` — сгенерированные отчёты
- `feedback/feedback.csv` — история фидбека
- `nemov_token.txt` — токен WB API (строка 2)
